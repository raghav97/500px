<% require 'json' %>

<% images = [] %>

<% response = F00px.get('photos?feature=fresh_week') %>
<% a = response.body %>
<% hash = JSON.parse(a) %>
<% hash["photos"].each do |e| %>
  <% images.push(e["image_url"]) %>
<% end %>

<% images.each do |e| %>
  <div class=""><img class="image" src= "<%= e %>" />
<% end %>

<%= render 'paginate' %>